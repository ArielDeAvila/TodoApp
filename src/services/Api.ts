import LoginEnpoint from "./Endpoints/Login";
import TasksEndpoints from "./Endpoints/Task";
import UserEndpoint from "./Endpoints/User";

const api = {
  LoginEnpoint,
  TasksEndpoints,
  UserEndpoint
}

export default api;